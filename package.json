{
  "name": "eventease",
  "version": "1.0.0",
  "private": true,
  "description": "EventEase - Campus Event Participation, Attendance & CC Credit Management System",
  "scripts": {
    "db": "npm --prefix server run db",
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
